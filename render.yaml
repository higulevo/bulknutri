services:
  - type: web
    name: bulknutri-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npx prisma generate && npx prisma db push && node src/seed.js
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
